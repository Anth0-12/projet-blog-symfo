<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>Bienvenue sur mon blog! - {% block title %}{% endblock %}</title>

        
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>

    <nav class="" style="height: 75px">
        <div class="container-fluid d-flex flex-wrap h-100 bg-primary align-items-center">
            
                <div class="dropdown">
                    <a class="nav nav-link dropdown-toggle text-white" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                       Mon blog
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="dropdownMenuButtonAdminDashboard">
                        <li><a class="dropdown-item" href="{{ path('admin') }}">Tableau de bord</a></li>
                        <li><a class="dropdown-item" href="#">Menus</a></li>
                    </ul>
                </div>

                <div class="dropdown">
                    <a class="nav nav-link dropdown-toggle text-white" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                       Ajouter
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="dropdownMenuButtonAdminAdd">
                        <li><a class="dropdown-item" href="#">Articles</a></li>
                        <li><a class="dropdown-item" href="#">Pages</a></li>
                    </ul>
                </div>

        </div>
    </nav>


<div class="container-fluid">
    <header class="d-flex align-items-center justify-content-center border-bottom" style="height: 75px">
    
        <a class="d-flex align-items-center justify-content-center mb-3 mb-md-0 col-9 text-dark text-decoration-none" href="{{ path('app_home') }}">
            <span class="fs-4 d-flex align-items-center justify-content-center">
                Mon blog
            </span>
        </a>

        <ul class="nav nav-pills align-items-center justify-content-center">
            <li class="nav-item">
                <a class="nav-link text-dark" href="#">S'inscrire</a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-dark" href="#">Se connecter</a>
            </li>
        </ul>

    </header>
</div>


    <body class="bg-secondary bg-opacity-25">
        {% block body %}{% endblock %}
    </body>

<div class="container">

    <footer class="d-flex flex-wrap justify-content-between align-items-center py-3 my-4 border-top">
        <div class="col-md-4 mb-0 align-items-center">
            <span class="copyright">
                © {{ 'now'|date('Y') }} Antho. Tous droits réservés
            </span>
        </div>

    </footer>
</div>
    
</html>
